{{-- Default Livewire 3 layout — delegates to the project's existing <x-app-layout>
     so Livewire full-page components get the same header, sidebar, footer,
     scripts, and theme as every other authenticated page. --}}
<x-app-layout>
    {{ $slot }}
</x-app-layout>
