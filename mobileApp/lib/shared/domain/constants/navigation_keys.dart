/// Named keys for navigator GlobalKeys registered in the service locator, so
/// services can navigate without a [BuildContext].
class NavigationKeys {
  NavigationKeys._();

  static const String global = 'global_navigator';
}
